# Markdown → Typst PDF (demo)

This repo keeps content in **Markdown** and renders consistent PDFs using **Typst**.

## Quick start
```bash
# 1) Install Typst: https://typst.app
# 2) Build all PDFs
scripts/build_pdfs.sh
# Outputs go to ./build
